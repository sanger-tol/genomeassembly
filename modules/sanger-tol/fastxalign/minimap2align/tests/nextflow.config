nextflow.enable.moduleBinaries = true

process {
    withName: FASTXALIGN_MINIMAP2ALIGN {
        ext.args1 = { "${params.minimap2_args}"      }
        ext.args2 = { "${params.samtools_view_args}" }
        ext.args3 = { "${params.samtools_sort_args}" }
    }
}
